# Environment variables for the Claw Plays Pokemon API

# Path to the Pokemon ROM file (for emulator container)
ROM_PATH=./roms/pokemon_firered.gba

# Path for save state persistence
SAVE_STATE_PATH=./data/pokemon.save

# API configuration
API_PORT=3000
API_URL=http://localhost:3000

# Emulator communication (UDP)
# Hostname of emulator container (use "emulator" in Docker, "localhost" for local dev)
EMULATOR_HOST=emulator
# UDP port for memory commands (READ_CORE_MEMORY, etc.)
EMULATOR_MEMORY_PORT=55355
# UDP port for input injection (button presses)
EMULATOR_INPUT_PORT=55400

# UDP reliability settings
UDP_TIMEOUT_MS=2000
UDP_RETRIES=3

# RTMP streaming URL
# For local testing with mediamtx: rtmp://rtmp:1935/live/stream
# For Twitch production: rtmp://live.twitch.tv/app/YOUR_STREAM_KEY
RTMP_URL=rtmp://rtmp:1935/live/stream

# Screenshot capture timeout (ms)
SCREENSHOT_TIMEOUT_MS=10000

# Polling intervals (ms)
# How often to capture screenshots from RTMP stream
SCREENSHOT_FETCH_INTERVAL_MS=5000
# How often to read game state from emulator RAM
GAMESTATE_FETCH_INTERVAL_MS=3000

# Voting configuration
VOTE_WINDOW_DURATION_MS=10000
