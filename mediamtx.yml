# MediaMTX configuration for Claw Plays Pokemon
# Run with: mediamtx mediamtx.yml

logLevel: debug

# CRITICAL: Increase timeouts for FFmpeg initialization delay
readTimeout: 30s
writeTimeout: 30s
writeQueueSize: 1024

# Accept any path dynamically (wildcard)
paths:
  all_others:
